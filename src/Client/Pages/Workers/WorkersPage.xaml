<Page x:Class="Mastemis.Client.Pages.Workers.WorkersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:Mastemis.Client.Controls">
    <ScrollViewer><StackPanel Padding="{StaticResource MastemisPageMargin}" Spacing="18" MaxWidth="1280">
        <controls:PageHeader Eyebrow="JUDGE OPERATIONS" Title="Workers" Subtitle="Sandboxed execution capacity and worker readiness."><controls:PageHeader.Commands><Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource MastemisSecondaryButtonStyle}"/></controls:PageHeader.Commands></controls:PageHeader>
        <InfoBar IsOpen="{Binding HasError}" Severity="Error" IsClosable="False" Title="Worker inventory unavailable" Message="{Binding Error}"/>
        <Grid ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><controls:MetricTile Label="Active workers" Value="{Binding Workers.Count}" Detail="Authorized inventory" Glyph="&#xE950;"/><controls:MetricTile Grid.Column="1" Label="Total capacity" Value="{Binding TotalCapacity}" Detail="Shared judge budget" Glyph="&#xE9D9;"/><controls:MetricTile Grid.Column="2" Label="Capacity used" Value="{Binding UsedCapacity}" Detail="Submission and reference jobs" Glyph="&#xE7BA;"/></Grid>
        <Border Background="{ThemeResource MastemisSurfaceBrush}" BorderBrush="{ThemeResource MastemisBorderBrush}" BorderThickness="1" CornerRadius="8"><StackPanel><TextBlock Padding="16,12" Text="Worker inventory" Style="{StaticResource MastemisSectionTitleStyle}"/><controls:EmptyState Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}" Title="No workers found" Message="No workers are visible in the current management scope." Glyph="&#xE770;"/><ListView ItemsSource="{Binding Workers}"><ListView.ItemTemplate><DataTemplate><Grid Padding="12,8" ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{Binding Name}" FontWeight="SemiBold"/><TextBlock Text="{Binding WorkerId}" Style="{StaticResource MastemisCodeTextStyle}"/></StackPanel><TextBlock Grid.Column="1" Text="{Binding SandboxBackend}"/><TextBlock Grid.Column="2" Text="{Binding ActiveJobs}"/><TextBlock Grid.Column="3" Text="{Binding CredentialStatus}"/></Grid></DataTemplate></ListView.ItemTemplate></ListView></StackPanel></Border>
    </StackPanel></ScrollViewer>
</Page>
