<Page x:Class="Mastemis.Client.Pages.ProblemStudio.ProblemStudioPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:Mastemis.Client.Controls">
    <Grid Padding="{StaticResource MastemisPageMargin}" RowSpacing="14">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <controls:PageHeader Eyebrow="PROBLEM AUTHORING" Title="Problem Studio" Subtitle="Edit MAS, validate deterministic generation, and monitor publication without leaving the document context.">
            <controls:PageHeader.Commands><Button Content="Refresh drafts" Command="{Binding RefreshCommand}"/><Button Content="Load selected" Command="{Binding LoadCommand}" Style="{StaticResource MastemisPrimaryButtonStyle}"/></controls:PageHeader.Commands>
        </controls:PageHeader>
        <Grid Grid.Row="1" ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <ComboBox Header="Authorized draft" ItemsSource="{Binding Drafts}" SelectedItem="{Binding SelectedDraft, Mode=TwoWay}" DisplayMemberPath="Title" MinWidth="360" HorizontalAlignment="Stretch"/>
            <Border Grid.Column="1" Padding="14,8" Background="{ThemeResource MastemisSubtleBrush}" BorderBrush="{ThemeResource MastemisBorderBrush}" BorderThickness="1" CornerRadius="6" VerticalAlignment="Bottom"><StackPanel Orientation="Horizontal" Spacing="8"><FontIcon Glyph="&#xE73E;" FontSize="14"/><TextBlock Text="{Binding Status}" VerticalAlignment="Center"/></StackPanel></Border>
        </Grid>
        <Grid Grid.Row="2" ColumnSpacing="14"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <ListView SelectionMode="Single" SelectedIndex="0" Background="{ThemeResource MastemisNavigationBrush}" CornerRadius="8" Padding="5">
                <ListViewItem Content="Overview"/><ListViewItem Content="Statements"/><ListViewItem Content="Assets" IsEnabled="False"/><ListViewItem Content="Judge settings"/><ListViewItem Content="MAS"/><ListViewItem Content="Reference solution" IsEnabled="False"/><ListViewItem Content="Generation"/><ListViewItem Content="Tests" IsEnabled="False"/><ListViewItem Content="Packages" IsEnabled="False"/><ListViewItem Content="Permissions" IsEnabled="False"/>
            </ListView>
            <TabView Grid.Column="1" IsAddTabButtonVisible="False">
                <TabViewItem Header="Overview">
                    <ScrollViewer><StackPanel Padding="18" Spacing="16"><TextBlock Text="Draft overview" Style="{StaticResource MastemisSectionTitleStyle}"/><Grid ColumnSpacing="24" RowSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><TextBlock Text="Problem" Style="{StaticResource MastemisMutedTextStyle}"/><TextBlock Grid.Column="1" Text="{Binding SelectedDraft.Title}"/><TextBlock Grid.Row="1" Text="Identifier" Style="{StaticResource MastemisMutedTextStyle}"/><TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedDraft.Id}" Style="{StaticResource MastemisCodeTextStyle}"/><TextBlock Grid.Row="2" Text="Revision" Style="{StaticResource MastemisMutedTextStyle}"/><TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedDraft.Version}"/><TextBlock Grid.Row="3" Text="Default locale" Style="{StaticResource MastemisMutedTextStyle}"/><TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedDraft.DefaultLocale}"/><TextBlock Grid.Row="4" Text="Checker" Style="{StaticResource MastemisMutedTextStyle}"/><TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedDraft.Checker}"/><TextBlock Grid.Row="5" Text="MAS" Style="{StaticResource MastemisMutedTextStyle}"/><TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Status}"/></Grid></StackPanel></ScrollViewer>
                </TabViewItem>
                <TabViewItem Header="Metadata">
                    <ScrollViewer><StackPanel Padding="18" Spacing="14"><Grid ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBox Header="Title" Text="{Binding Metadata.Title, Mode=TwoWay}"/><ComboBox Grid.Column="1" Header="Difficulty" SelectedValue="{Binding Metadata.Difficulty, Mode=TwoWay}"><ComboBoxItem Content="Easy"/><ComboBoxItem Content="Medium"/><ComboBoxItem Content="Hard"/></ComboBox></Grid><TextBox Header="Authors (comma separated)" Text="{Binding Metadata.Authors, Mode=TwoWay}"/><TextBox Header="Tags (comma separated)" Text="{Binding Metadata.Tags, Mode=TwoWay}"/><Grid ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBox Header="Default locale" Text="{Binding Metadata.DefaultLocale, Mode=TwoWay}"/><TextBox Grid.Column="1" Header="Languages" Text="{Binding Metadata.Languages, Mode=TwoWay}"/><ComboBox Grid.Column="2" Header="Checker" SelectedValue="{Binding Metadata.Checker, Mode=TwoWay}"><ComboBoxItem Content="exact"/><ComboBoxItem Content="tokens"/></ComboBox></Grid><Grid ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><NumberBox Header="Time limit (ms)" Value="{Binding Metadata.TimeLimit, Mode=TwoWay}"/><NumberBox Grid.Column="1" Header="Memory limit (bytes)" Value="{Binding Metadata.MemoryLimit, Mode=TwoWay}"/><NumberBox Grid.Column="2" Header="Output limit (bytes)" Value="{Binding Metadata.OutputLimit, Mode=TwoWay}"/></Grid><StackPanel Orientation="Horizontal" Spacing="10"><Button Content="Save metadata" Command="{Binding Metadata.SaveCommand}" Style="{StaticResource MastemisPrimaryButtonStyle}"/><TextBlock Text="{Binding Metadata.Status}" VerticalAlignment="Center"/></StackPanel></StackPanel></ScrollViewer>
                </TabViewItem>
                <TabViewItem Header="Statements">
                    <Grid Padding="14" ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="220"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><StackPanel Spacing="10"><TextBlock Text="Locales" Style="{StaticResource MastemisSectionTitleStyle}"/><ListView ItemsSource="{Binding Statements.Locales}" SelectedItem="{Binding Statements.Selected, Mode=TwoWay}" DisplayMemberPath="Locale"/><Button Content="Refresh locales" Command="{Binding Statements.RefreshCommand}"/><Button Content="Load selected" Command="{Binding Statements.LoadCommand}"/><TextBox Header="New locale" Text="{Binding Statements.Locale, Mode=TwoWay}"/><Button Content="Delete selected" Command="{Binding Statements.DeleteCommand}"/></StackPanel><ScrollViewer Grid.Column="1"><StackPanel Spacing="10"><TextBox Header="Localized title" Text="{Binding Statements.Title, Mode=TwoWay}"/><TextBox Header="Markdown statement" Text="{Binding Statements.Markdown, Mode=TwoWay}" AcceptsReturn="True" MinHeight="220" FontFamily="Cascadia Mono,Consolas,monospace"/><TextBox Header="Input description" Text="{Binding Statements.InputDescription, Mode=TwoWay}" AcceptsReturn="True"/><TextBox Header="Output description" Text="{Binding Statements.OutputDescription, Mode=TwoWay}" AcceptsReturn="True"/><TextBox Header="Constraints" Text="{Binding Statements.Constraints, Mode=TwoWay}" AcceptsReturn="True"/><TextBox Header="Notes" Text="{Binding Statements.Notes, Mode=TwoWay}" AcceptsReturn="True"/><StackPanel Orientation="Horizontal" Spacing="10"><Button Content="Save statement" Command="{Binding Statements.SaveCommand}" Style="{StaticResource MastemisPrimaryButtonStyle}"/><TextBlock Text="{Binding Statements.Status}" VerticalAlignment="Center"/></StackPanel></StackPanel></ScrollViewer></Grid>
                </TabViewItem>
                <TabViewItem Header="MAS source">
                    <Grid Padding="14" RowSpacing="10"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Deterministic generator" Style="{StaticResource MastemisSectionTitleStyle}"/><TextBlock Text="Source is bounded and never executed as native code." Style="{StaticResource MastemisMutedTextStyle}"/></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8"><Button Content="Save" Command="{Binding SaveMasCommand}"/><Button Content="Validate" Command="{Binding ValidateCommand}" Style="{StaticResource MastemisPrimaryButtonStyle}"/></StackPanel></Grid>
                        <TextBox Grid.Row="1" Text="{Binding Source, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" FontFamily="Cascadia Mono,Consolas,monospace" FontSize="14" TextWrapping="NoWrap" Background="{ThemeResource MastemisEditorBrush}" AutomationProperties.Name="MAS source editor"/>
                        <Grid Grid.Row="2" ColumnSpacing="10"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBox Width="120" Header="Seed" Text="{Binding Seed, Mode=TwoWay}"/><Button Grid.Column="1" VerticalAlignment="Bottom" Content="Start generation" Command="{Binding GenerateCommand}" Style="{StaticResource MastemisPrimaryButtonStyle}"/><TextBlock Grid.Column="2" VerticalAlignment="Bottom" Text="Diagnostics include stable codes and source locations when available." Style="{StaticResource MastemisCaptionTextStyle}" TextWrapping="Wrap"/></Grid>
                    </Grid>
                </TabViewItem>
                <TabViewItem Header="Diagnostics">
                    <ListView ItemsSource="{Binding Diagnostics}"><ListView.ItemTemplate><DataTemplate><Grid Padding="10,8"><Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Code}" FontWeight="SemiBold"/><TextBlock Grid.Column="1" Text="{Binding Message}" TextWrapping="Wrap"/></Grid></DataTemplate></ListView.ItemTemplate></ListView>
                </TabViewItem>
                <TabViewItem Header="Generation">
                    <StackPanel Padding="18" Spacing="14"><TextBlock Text="Operation progress" Style="{StaticResource MastemisSectionTitleStyle}"/><TextBlock Text="{Binding ProgressText}" FontSize="20" FontWeight="SemiBold"/><ProgressBar Value="{Binding Progress.Numerator}" Maximum="{Binding Progress.Denominator}"/><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="160"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Reference worker" Style="{StaticResource MastemisMutedTextStyle}"/><TextBlock Grid.Column="1" Text="{Binding ReferenceStatus}"/></Grid><StackPanel Orientation="Horizontal" Spacing="8"><Button Content="Refresh operation" Command="{Binding RefreshGenerationCommand}"/><Button Content="Cancel" Command="{Binding CancelGenerationCommand}"/></StackPanel></StackPanel>
                </TabViewItem>
            </TabView>
        </Grid>
        <InfoBar Grid.Row="3" IsOpen="{Binding HasError}" Severity="Error" Title="Problem Studio request failed" Message="{Binding Error}" IsClosable="False"/>
    </Grid>
</Page>
